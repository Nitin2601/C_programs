#include <stdio.h>

int main()
{
    int i,n;
    printf("Enter the value for n: ");
    scanf("%d",&n);
    printf("\nThe reverse value of ℕ from %d:\n",n);
    for(i=n;i>=1;i--)
    printf("%d\n",i);
    return 0;
}
